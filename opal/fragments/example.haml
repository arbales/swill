Musing about an alternate way to write the markup given the
server and client language are the same, and potentially change
the way that awakening works.

%main{controller: HelloController}
  %h1{bind: 'greeting'}
  %input{bind: 'user.name'}
